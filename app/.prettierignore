# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/
/build/
/.svelte-kit/

# The Rust half — `cargo fmt` owns everything under it, including its JSON.
/src-tauri/

# Generated by Tauri Specta (`pnpm gen:bindings`) — committed, but keep it
# byte-identical to the generator's output or every regen shows as a diff.
/src/lib/bindings.ts

# Regenerated by Paraglide on every install (prepare → i18n:compile);
# only project.inlang/settings.json and messages/*.json are ours.
/src/lib/paraglide/
/project.inlang/cache/

# Stale output from an older Paraglide outdir; unreferenced, safe to delete.
/src/paraglide/

# Scratch dir for probing what Tailwind actually emits.
/.tw-probe/
