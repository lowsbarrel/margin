# Nightly-only options are deliberately absent: CI formats on stable, where such a key silently does nothing.
edition = "2024"
style_edition = "2024"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
